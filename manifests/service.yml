apiVersion: v1
kind: Service
metadata:
    name: "instarentaks-bd1f"
    labels:
        app: "instarentaks-bd1f"
spec:
    type: LoadBalancer
    ports:
    - port: 80
      targetPort: 80
      protocol: TCP
      name: http
    selector:
        app: "instarentaks-bd1f"